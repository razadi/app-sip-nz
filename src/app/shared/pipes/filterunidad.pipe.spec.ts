import { FilterunidadPipe } from './filterunidad.pipe';

describe('FilterunidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterunidadPipe();
    expect(pipe).toBeTruthy();
  });
});
